var expect   = require('chai').expect;
var assert   = require('chai').assert;
var request  = require('supertest');
// var config   = require('../../server/config.json');
var mongoose = require('mongoose');

describe('Adding Data To Mongo', function(){
  
  xit('should not be able to post empty fields to db', function(done) { 
  
  });

  xit('should not be able to post duplicate grump commands to db', function(done) { 
  
  });

});

describe('Retriving Data From Mongo', function(){
  xit('should find data when queried by grump command', function() {
  
  });

  xit('should index data based on grump command', function() {
  
  });

});